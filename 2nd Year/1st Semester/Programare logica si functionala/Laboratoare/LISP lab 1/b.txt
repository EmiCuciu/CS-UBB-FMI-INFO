succesor(l:Lista, Rez: lista rezultat)
(i,o) model de flux

Model matematic

succesor(l1 l2 .. ln) = reverse(add_one(reverse(l1 l2 .. ln)))

add_one(l1 l2 .. ln) =  { (1), n=0}
                        { (sum (+) add_one(l2 l3 .. ln)) , unde sum = l1+1 si sum<10}
                        { (0 (+) add_one(l2 l3 .. ln), altfel, adica sum>=10)}